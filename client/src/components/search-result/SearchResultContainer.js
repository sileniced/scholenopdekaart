import * as React from "react";
import { connect } from "react-redux";

class SearchResultContainer extends React.PureComponent {
  render() {
    return <div />;
  }
}

const mapStateToProps = state => ({});

export default connect(mapStateToProps)(EventDisplayContainer);
